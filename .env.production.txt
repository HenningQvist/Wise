# ========================================
# SERVER
# ========================================
NODE_ENV=production
PORT=8080                     # Railway/Render tilldelar oftast PORT automatiskt
HTTPS=false                   # Produktion hanterar HTTPS externt

# ========================================
# FRONTEND & API
# ========================================
VITE_API_URL=wise-production-2cc4.up.railway.app/api
FRONTEND_URL=https://wisemate.netlify.app
ALLOWED_ORIGINS=https://wisemate.netlify.app

# ========================================
# DATABASE (Railway / annan host)
# ========================================
DB_USER=${DB_USER}
DB_PASS=${DB_PASS}
DB_HOST=${DB_HOST}
DB_NAME=${DB_NAME}
DB_PORT=${DB_PORT}

# ========================================
# JWT
# ========================================
JWT_SECRET=${JWT_SECRET}
